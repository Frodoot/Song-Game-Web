"use strict";
/**
 * Custom error classes and error handling utilities for whisper-node
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ModelNotFoundError = exports.FileNotFoundError = exports.SecurityError = exports.ValidationError = exports.WhisperNodeError = void 0;
exports.safeAsync = safeAsync;
exports.safeSync = safeSync;
exports.isWhisperNodeError = isWhisperNodeError;
class WhisperNodeError extends Error {
    constructor(message, code) {
        super(message);
        this.code = code;
        this.name = 'WhisperNodeError';
    }
}
exports.WhisperNodeError = WhisperNodeError;
class ValidationError extends WhisperNodeError {
    constructor(message) {
        super(message, 'VALIDATION_ERROR');
        this.name = 'ValidationError';
    }
}
exports.ValidationError = ValidationError;
class SecurityError extends WhisperNodeError {
    constructor(message) {
        super(message, 'SECURITY_ERROR');
        this.name = 'SecurityError';
    }
}
exports.SecurityError = SecurityError;
class FileNotFoundError extends WhisperNodeError {
    constructor(filePath) {
        super(`File not found: ${filePath}`, 'FILE_NOT_FOUND');
        this.name = 'FileNotFoundError';
    }
}
exports.FileNotFoundError = FileNotFoundError;
class ModelNotFoundError extends WhisperNodeError {
    constructor(modelName) {
        super(`Model not found: ${modelName}. Run 'npx whisper-node download' to fetch models.`, 'MODEL_NOT_FOUND');
        this.name = 'ModelNotFoundError';
    }
}
exports.ModelNotFoundError = ModelNotFoundError;
/**
 * Safely execute an async operation with proper error handling
 * @param operation The async operation to execute
 * @param errorMessage Custom error message prefix
 * @returns Promise that resolves to the operation result or rejects with a WhisperNodeError
 */
function safeAsync(operation, errorMessage) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            return yield operation();
        }
        catch (error) {
            const message = error instanceof Error ? error.message : String(error);
            throw new WhisperNodeError(`${errorMessage}: ${message}`);
        }
    });
}
/**
 * Safely execute a synchronous operation with proper error handling
 * @param operation The sync operation to execute
 * @param errorMessage Custom error message prefix
 * @returns The operation result or throws a WhisperNodeError
 */
function safeSync(operation, errorMessage) {
    try {
        return operation();
    }
    catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        throw new WhisperNodeError(`${errorMessage}: ${message}`);
    }
}
/**
 * Check if an error is a known whisper-node error type
 * @param error The error to check
 * @returns True if it's a WhisperNodeError or subclass
 */
function isWhisperNodeError(error) {
    return error instanceof WhisperNodeError;
}
