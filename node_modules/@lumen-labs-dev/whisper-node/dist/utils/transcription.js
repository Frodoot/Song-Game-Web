"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = parseTranscript;
exports.looksLikeWordLevelTranscript = looksLikeWordLevelTranscript;
exports.mergeWordLevelTranscript = mergeWordLevelTranscript;
/**
 * Parse whisper.cpp VTT-like console output into structured transcript lines.
 * Lines are expected in the form: "[HH:MM:SS.mmm --> HH:MM:SS.mmm]  text".
 * Returns an empty array for empty or unmatched input.
 */
function parseTranscript(vtt) {
    if (!vtt)
        return [];
    const matches = vtt.match(/\[[0-9:.]+\s-->\s[0-9:.]+\][^\n]*/g) || [];
    const results = [];
    for (const raw of matches) {
        const line = raw.trim();
        const m = line.match(/^\[([0-9:.]+)\s-->\s([0-9:.]+)\]\s+(.*)$/);
        if (!m)
            continue;
        const start = m[1];
        const end = m[2];
        const speech = m[3].replace(/\r?\n/g, "").trimStart();
        results.push({ start, end, speech });
    }
    return results;
}
/**
 * Heuristic: does the transcript look like it is split per word?
 */
function looksLikeWordLevelTranscript(lines) {
    if (!lines || lines.length < 4)
        return false;
    let wordLike = 0;
    for (const l of lines) {
        if (!/\s/.test(l.speech))
            wordLike++;
    }
    return wordLike / lines.length > 0.8; // mostly single tokens
}
function pad2(n) {
    return n < 10 ? "0" + String(n) : String(n);
}
function pad3(n) {
    if (n < 10)
        return "00" + String(n);
    if (n < 100)
        return "0" + String(n);
    return String(n);
}
function timeToMs(t) {
    const parts = t.split(":");
    if (parts.length !== 3)
        return 0;
    const hours = Number(parts[0]) || 0;
    const minutes = Number(parts[1]) || 0;
    const seconds = Number(parts[2]) || 0; // supports SS.mmm
    const totalSeconds = hours * 3600 + minutes * 60 + seconds;
    return Math.round(totalSeconds * 1000);
}
function msToTime(ms) {
    if (ms < 0)
        ms = 0;
    const totalSeconds = Math.floor(ms / 1000);
    const milli = ms - totalSeconds * 1000;
    const hours = Math.floor(totalSeconds / 3600);
    const minutes = Math.floor((totalSeconds % 3600) / 60);
    const seconds = totalSeconds % 60;
    return `${pad2(hours)}:${pad2(minutes)}:${pad2(seconds)}.${pad3(Math.round(milli))}`;
}
function isPunctuation(token) {
    if (!token)
        return false;
    // Common punctuation tokens as emitted by whisper.cpp with word timestamps
    return /^[.,!?;:\-–—…()\[\]{}"'`]+$/.test(token);
}
function isEndSentencePunct(token) {
    return /[.!?]$/.test(token);
}
/**
 * Merge per-word ITranscriptLine entries into sentence/segment-level entries
 * using simple punctuation and pause heuristics.
 */
function mergeWordLevelTranscript(lines, opts) {
    var _a, _b;
    if (!lines || lines.length === 0)
        return [];
    const maxGapMs = (_a = opts === null || opts === void 0 ? void 0 : opts.maxGapMs) !== null && _a !== void 0 ? _a : 600; // 0.6s pause => break
    const maxChars = (_b = opts === null || opts === void 0 ? void 0 : opts.maxCharsPerSegment) !== null && _b !== void 0 ? _b : 320;
    const merged = [];
    let buffer = [];
    let segStartMs = null;
    let segEndMs = null;
    let currentSpeaker = undefined;
    const flush = () => {
        if (buffer.length === 0 || segStartMs === null || segEndMs === null)
            return;
        const text = buffer.join("");
        const item = {
            start: msToTime(segStartMs),
            end: msToTime(segEndMs),
            speech: text.trim(),
        };
        if (currentSpeaker)
            item.speaker = currentSpeaker;
        merged.push(item);
        buffer = [];
        segStartMs = null;
        segEndMs = null;
        currentSpeaker = undefined;
    };
    for (let i = 0; i < lines.length; i++) {
        const cur = lines[i];
        const next = lines[i + 1];
        const curStart = timeToMs(cur.start);
        const curEnd = timeToMs(cur.end);
        if (segStartMs === null)
            segStartMs = curStart;
        segEndMs = curEnd;
        if (!currentSpeaker && cur.speaker)
            currentSpeaker = cur.speaker;
        const token = cur.speech;
        if (buffer.length === 0) {
            buffer.push(token);
        }
        else {
            if (isPunctuation(token)) {
                buffer.push(token);
            }
            else {
                const prev = buffer[buffer.length - 1];
                if (prev && isPunctuation(prev))
                    buffer.push(" " + token);
                else
                    buffer.push(" " + token);
            }
        }
        const reachedMaxChars = buffer.join("").length >= maxChars;
        const endedWithEOS = isEndSentencePunct(token);
        let gapBreak = false;
        if (next) {
            const nextStart = timeToMs(next.start);
            const gap = nextStart - curEnd;
            gapBreak = gap > maxGapMs;
        }
        if (endedWithEOS || gapBreak || reachedMaxChars || !next) {
            flush();
        }
    }
    return merged;
}
