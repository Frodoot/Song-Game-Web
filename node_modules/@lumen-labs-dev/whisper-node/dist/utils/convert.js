"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ensureWav16kMono = ensureWav16kMono;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const fluent_ffmpeg_1 = __importDefault(require("fluent-ffmpeg"));
const ffmpeg_static_1 = __importDefault(require("ffmpeg-static"));
const ffprobe_static_1 = __importDefault(require("ffprobe-static"));
const constants_1 = require("../config/constants");
fluent_ffmpeg_1.default.setFfmpegPath(ffmpeg_static_1.default || "ffmpeg");
fluent_ffmpeg_1.default.setFfprobePath(ffprobe_static_1.default.path || "ffprobe");
/**
 * Ensure an input audio file is a 16 kHz mono WAV. If it isn't, convert it.
 * - Returns the original path if already compliant
 * - Otherwise, writes a sibling file with suffix .wav16k.wav and returns that path
 */
function ensureWav16kMono(inputPath) {
    return __awaiter(this, void 0, void 0, function* () {
        const normalized = path_1.default.normalize(inputPath);
        const isCompliant = yield isWav16kMono(normalized);
        if (isCompliant)
            return normalized;
        const { dir, name } = path_1.default.parse(normalized);
        const outPath = path_1.default.join(dir, `${name}.wav16k.wav`);
        yield convertToWav16kMono(normalized, outPath);
        return outPath;
    });
}
function isWav16kMono(p) {
    return __awaiter(this, void 0, void 0, function* () {
        return new Promise((resolve) => {
            try {
                fluent_ffmpeg_1.default.ffprobe(p, (err, data) => {
                    if (err || !data)
                        return resolve(false);
                    const stream = (data.streams || []).find((s) => s.codec_type === "audio");
                    if (!stream)
                        return resolve(false);
                    const codec = (stream.codec_name || "").toLowerCase();
                    const sampleRate = Number(stream.sample_rate || 0);
                    const channels = Number(stream.channels || 0);
                    const isWav = codec === "pcm_s16le" || codec === "pcm_s24le" || codec === "pcm_s32le";
                    const ok = isWav && sampleRate === constants_1.AUDIO_CONSTANTS.SAMPLE_RATE && channels === 1;
                    resolve(ok);
                });
            }
            catch (_a) {
                resolve(false);
            }
        });
    });
}
function convertToWav16kMono(inputPath, outputPath) {
    return __awaiter(this, void 0, void 0, function* () {
        const outDir = path_1.default.dirname(outputPath);
        if (!fs_1.default.existsSync(outDir))
            fs_1.default.mkdirSync(outDir, { recursive: true });
        yield new Promise((resolve, reject) => {
            (0, fluent_ffmpeg_1.default)()
                .input(inputPath)
                .audioCodec("pcm_s16le")
                .audioChannels(1)
                .audioFrequency(constants_1.AUDIO_CONSTANTS.SAMPLE_RATE)
                .format("wav")
                .on("error", (err) => reject(err))
                .on("end", () => resolve())
                .save(outputPath);
        });
    });
}
exports.default = ensureWav16kMono;
