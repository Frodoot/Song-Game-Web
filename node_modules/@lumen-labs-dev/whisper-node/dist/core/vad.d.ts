export type Frame = {
    start: number;
    end: number;
    energy: number;
};
export type VoicedFrame = Frame & {
    idx: number;
};
/**
 * Extract 16 kHz mono PCM from an input audio file into memory.
 */
export declare function extractPCM16(inputPath: string): Promise<Float32Array>;
/**
 * Compute per-frame log-energy features.
 */
export declare function computeFrameEnergies(pcm: Float32Array, sampleRate: number, frameMs: number): Frame[];
export declare function median(xs: number[]): number;
/**
 * Compute VAD threshold as median(energy) * multiplier.
 */
export declare function computeVadThreshold(frames: Frame[], multiplier?: number): number;
/**
 * Filter frames by threshold and attach their original index.
 */
export declare function filterVoicedFrames(frames: Frame[], threshold: number): VoicedFrame[];
