/**
 * Default Whisper model name used when no model is provided by the caller.
 * Matches a filename in `lib/whisper.cpp/models` via the model map.
 */
export declare const DEFAULT_MODEL = "base.en";
/**
 * Absolute path to the bundled Whisper models directory inside this package.
 * Resolved relative to the compiled JS output directory to work from npm installs.
 */
export declare const MODELS_PATH: string;
/**
 * Absolute path to the bundled Whisper scripts directory inside this package.
 * Resolved relative to the compiled JS output directory to work from npm installs.
 */
export declare const SCRIPTS_PATH: string;
/**
 * Root path to bundled whisper.cpp directory.
 */
export declare const WHISPER_CPP_PATH: string;
/**
 * Model downloader script filenames used by whisper.cpp
 */
export declare const MODEL_SCRIPT_FILENAMES: {
    readonly posix: "download-ggml-model.sh";
    readonly windowsCmd: "download-ggml-model.cmd";
};
/**
 * Configuration constants for diarization
 */
export declare const DIARIZATION_CONSTANTS: {
    readonly DEFAULT_FRAME_MS: 20;
    readonly DEFAULT_VAD_MULTIPLIER: 1.5;
    readonly MIN_ENERGY_OFFSET: 1e-9;
    readonly MAX_SPEAKERS: 6;
    readonly MIN_SPEAKERS: 1;
};
/**
 * Download and network constants
 */
export declare const NETWORK_CONSTANTS: {
    readonly MAX_REDIRECTS: 5;
    readonly DOWNLOAD_TIMEOUT_MS: 300000;
};
/**
 * Audio processing constants
 */
export declare const AUDIO_CONSTANTS: {
    readonly SAMPLE_RATE: 16000;
    readonly WAV_HEADER_SIZE: 44;
    readonly PCM_SCALE_FACTOR: 32768;
};
