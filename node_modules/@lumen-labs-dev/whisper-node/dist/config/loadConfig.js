"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadConfig = loadConfig;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const DEFAULT_CONFIG_BASENAMES = [
    "whisper-node.config.json",
    "whisper.config.json",
];
function findConfigPath() {
    const fromEnv = process.env.WHISPER_NODE_CONFIG;
    if (fromEnv) {
        try {
            const p = path_1.default.resolve(fromEnv);
            if (fs_1.default.existsSync(p))
                return p;
        }
        catch (_a) { }
    }
    const cwd = process.cwd();
    for (const base of DEFAULT_CONFIG_BASENAMES) {
        const p = path_1.default.join(cwd, base);
        if (fs_1.default.existsSync(p))
            return p;
    }
    return null;
}
function loadConfig() {
    const configPath = findConfigPath();
    if (!configPath)
        return {};
    try {
        const raw = fs_1.default.readFileSync(configPath, "utf8");
        const json = JSON.parse(raw);
        const cfg = {};
        if (typeof json.modelName === "string")
            cfg.modelName = json.modelName;
        if (typeof json.modelPath === "string")
            cfg.modelPath = json.modelPath;
        if (typeof json.whisperOptions === "object")
            cfg.whisperOptions = json.whisperOptions;
        if (typeof json.shellOptions === "object")
            cfg.shellOptions = json.shellOptions;
        return cfg;
    }
    catch (err) {
        console.log("[whisper-node] Failed to parse config. Ignoring.");
        return {};
    }
}
exports.default = loadConfig;
//# sourceMappingURL=loadConfig.js.map